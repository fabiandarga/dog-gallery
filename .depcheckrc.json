{
  "ignoreMatches": [
    "@babel/*",  // Ignore all babel packages
    "webpack-*", // Ignore webpack related packages
    "autoprefixer",
    "postcss"
  ],
  "specials": [
    "babel",  // Parse babel config
    "webpack" // Parse webpack config
  ]
}
